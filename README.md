# Test-Job-Public

Publieke updatebron voor `Test-Job.bat`.

Deze repository bevat alleen `Test-Job-online.txt`, zodat clients altijd de laatste PowerShell-versie kunnen ophalen via een publieke tekst-URL.
